{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Region creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('crud_region') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
